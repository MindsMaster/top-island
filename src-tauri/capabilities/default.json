{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "island and settings window permissions",
  "windows": ["island", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
